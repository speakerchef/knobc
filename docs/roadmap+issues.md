- [ ] Parenthesized expressions with `sub` & `add` op `(1 + 2) - 4` fail
- [ ] No unary negation
- [ ] No working comments
- [ ] No function call parsing -- **PRIORITY!!**

- [ ] Refactor to function-like state where main is a function and not implicit. i.e. codegenerator structs for each individual function.

- [ ] Generate default behavior and expect main to be defined for program to start

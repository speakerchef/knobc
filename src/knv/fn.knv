fn main() -> void {
    let x = 50;
    let y = 90;
    let res = x + y;
    if res > 50 {
        exit res;
    } else {
        foo(69);
    }
}

fn foo(exit_code: i32) -> void {
    exit exit_code;
}
